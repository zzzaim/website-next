# Blank React+Next.js Project

With only `next`, `react`, `react-dom`, and ESLint packages (with recommended configuration).

With basic `scripts` configuration of Next.js tools.

With default blank `pages/index.js` page.
